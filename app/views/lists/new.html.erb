<!DOCTYPE html>
<html>
  <head>
    <title>TodoList</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>

    <!-- Estilos diretamente no arquivo HTML -->
    <style>
      /* Estilo para o título */
      h1.page-title {
        font-size: 2rem;
        text-align: center;
        color: #333;
        margin-bottom: 20px;
      }

      /* Estilo para o formulário */
      form {
        background-color: #f9f9f9;
        padding: 20px;
        border-radius: 8px;
        box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        max-width: 600px;
        margin: 0 auto;
      }

      /* Estilo para os campos do formulário */
      form div {
        margin-bottom: 15px;
      }

      form label {
        display: block;
        font-size: 1.1rem;
        color: #555;
        margin-bottom: 5px;
      }

      form input[type="text"],
      form textarea {
        width: 100%;
        padding: 10px;
        font-size: 1rem;
        border: 1px solid #ccc;
        border-radius: 4px;
        box-sizing: border-box;
      }

      form textarea {
        resize: vertical;
        height: 150px;
      }

      /* Estilo para o botão de envio */
      form input[type="submit"] {
        background-color: #4CAF50;
        color: white;
        padding: 10px 20px;
        font-size: 1rem;
        border: none;
        border-radius: 4px;
        cursor: pointer;
        width: 100%;
      }

      form input[type="submit"]:hover {
        background-color: #45a049;
      }

      /* Estilo para os erros */
      div[style="color: red"] {
        background-color: #f8d7da;
        padding: 15px;
        border-radius: 4px;
        margin-bottom: 20px;
      }

      /* Estilo para o botão de Voltar */
      a.btn-back {
        display: inline-block;
        background-color: #6c757d;
        color: white;
        padding: 12px 30px;
        font-size: 16px;
        font-weight: 500;
        border-radius: 30px;
        text-align: center;
        text-decoration: none;
        box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        transition: all 0.3s ease;
      }

      a.btn-back:hover {
        background-color: #5a6268;
        box-shadow: 0 6px 10px rgba(0, 0, 0, 0.15);
      }

      a.btn-back:active {
        background-color: #545b62;
      }

      a.btn-back i {
        margin-right: 8px;
      }

      /* Estilo para o botão de Voltar */
      a.btn-outline-secondary {
        display: inline-block;
        background-color: #f8f9fa;
        color: #6c757d;
        padding: 12px 25px;
        font-size: 16px;
        font-weight: 600;
        border: 2px solid #6c757d;
        border-radius: 30px;
        text-align: center;
        text-decoration: none;
        box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        transition: all 0.3s ease;
      }

      a.btn-outline-secondary:hover {
        background-color: #6c757d;
        color: white;
        box-shadow: 0 6px 12px rgba(0, 0, 0, 0.15);
      }

      a.btn-outline-secondary:active {
        background-color: #5a6268;
        color: white;
      }

      a.btn-outline-secondary i {
        margin-right: 8px;
      }

      .header-container {
        position: absolute;
        top: 20px;
        right: 20px;
      }
    </style>
  </head>

  <body>
    <h1 class="page-title">Create a New Task List</h1>

    <div class="header-container">
      <%= link_to 'Back', lists_path, class: 'btn btn-outline-secondary btn-back' %>
    </div>

    <%= form_with model: @list, local: true, class: 'task-list-form' do |form| %>
      <% if @list.errors.any? %>
        <div class="alert alert-danger">
          <h2><%= pluralize(@list.errors.count, "error") %> prohibited this list from being saved:</h2>
          <ul>
            <% @list.errors.full_messages.each do |msg| %>
              <li><%= msg %></li>
            <% end %>
          </ul>
        </div>
      <% end %>

      <div class="form-group">
        <%= form.label :name, class: 'form-label' %>
        <%= form.text_field :name, class: 'form-control', placeholder: "Enter list name" %>
      </div>

      <div class="form-group">
        <%= form.label :description, "Description", class: 'form-label' %>
        <%= form.text_area :description, placeholder: "Optional description for your list", class: 'form-control' %>
      </div>

      <div class="form-group">
        <%= form.submit 'Create List', class: 'btn btn-primary btn-submit' %>
      </div>
    <% end %>
  </body>
</html>
